<div class="container">
    <table class="table table-striped">
        <tr>
            <th> Id </th><th> Nome </th>
        </tr>
        {{#each cursos.rows}}        
        <tr>
            <td> {{this.id}} </td><td> {{this.nome}} </td>
        </tr>
        {{/each}}
    </table>
    <p>  </p>
</div>